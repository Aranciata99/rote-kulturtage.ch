const cScroller = document.getElementById('programmScroller');
const cBack = document.getElementById('crowdFoundingBackground');

document.addEventListener('DOMContentLoaded', () => {
    cScroller.classList.add('open');
    cBack.classList.add('open');
});